<p id="notice"><%= notice %></p>

<div class="text-center">
  <h2 class="page-headline"><%= @organization.name %> Departments</h2>
<p>
  <h4 class="text-center page-headline"><%= @organization.description %></h4>
</p>
  <span class="org-url text-left">
    <% unless @organization.img_url == nil %>
      <%= image_tag @organization.img_url, size: "100x70" %>
    <% end %>
  </span>
</div>

  <!-- Button trigger modal -->
  <div class="text-center dept-btn">
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#myModal">
      New Department
    </button>
  </div><br>

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="myModalLabel">Add Category</h4>
        </div>
        <div class="modal-body">
          <%= render 'departments/form' %>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>

<div class="table-card">
  <table class="table">
    <thead>
      <tr>
        <th class="text-center">Department</th>
        <!-- <th class="text-center">Organization</th> -->
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @organization.departments.order(:name).each do |department| %>
        <tr>
          <td class="text-center"><%= link_to department.name, department_path(department) %></td>
          <!--td class="text-center"><%= department.organization.name %></td -->
          <td><%= link_to '', edit_department_path(department), class: "glyphicon glyphicon-edit" %></td>
          <td><%= link_to '', department, method: :delete, data: { confirm: 'Are you sure?' }, class: "glyphicon glyphicon-trash" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<br>

<%# render 'departments/form' %>


  <br>
  <div class="text-center">
    <%= link_to 'Edit', edit_organization_path(@organization) %>
    <%# link_to 'Back', organizations_path %>
  </div>
</div>
