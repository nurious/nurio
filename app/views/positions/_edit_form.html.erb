<%= form_for([@department, @position]) do |f| %>
<%# f.hidden_field :department_id, value: @position.department %>
  <% if @position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% @position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <!-- <div class="field"> -->
    <%# f.label :department_id %><br>
    <%# f.text_field :department_id %>
  <!-- </div> -->
  <div class="actions">
    <%= f.submit class: "btn btn-primary update-btn" %>
  </div>
<% end %>
